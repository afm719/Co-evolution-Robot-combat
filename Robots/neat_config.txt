[NEAT]
# Parámetros generales
fitness_criterion     = max
fitness_threshold     = 0.0
pop_size              = 150
reset_on_extinction   = False

[DefaultGenome]
# Configuración del genoma
num_inputs            = 2
num_outputs           = 1
num_hidden            = 1
initial_connection    = full  # Todas las conexiones inicialmente activas
activation_default    = sigmoid  # Función de activación predeterminada
aggregation_default   = sum  # Tipo de agregación por defecto
bias_init_mean        = 0.0
bias_init_stdev       = 1.0
weight_init_mean      = 0.0
weight_init_stdev     = 1.0
weight_max_value      = 8.0
weight_min_value      = -8.0
weight_mutate_rate    = 0.75
node_add_prob         = 0.03
node_delete_prob      = 0.03
connection_add_prob   = 0.05
connection_delete_prob = 0.05
feed_forward          = True
bias_replace_rate     = 0.2  # Tasa de reemplazo de los sesgos
bias_mutate_rate      = 0.1  # Tasa de mutación de los sesgos
bias_mutate_power     = 0.5  # Poder de la mutación de los sesgos

# Parámetros de compatibilidad entre genomas
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient = 0.5
compatibility_threshold = 3.0  # Umbral de compatibilidad entre genomas

# Reproducción
conn_add_prob          = 0.25
conn_delete_prob       = 0.25
# Mutaciones
weight_mutate_power    = 0.5  # Tasa de mutación de pesos
bias_mutate_power      = 0.5  # Poder de mutación de sesgos
weight_mutate_rate     = 0.8  # Tasa de mutación de los pesos
bias_mutate_rate       = 0.2  # Tasa de mutación de los sesgos
